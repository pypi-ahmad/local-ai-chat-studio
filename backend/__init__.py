"""Local AI Chat Studio v2 backend.

The FastAPI web layer lives in backend/app (start at backend/app/main.py);
this package itself holds no logic. Domain logic lives in the sibling src/
package.
"""
